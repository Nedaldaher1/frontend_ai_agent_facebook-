import { KnowledgeForm } from "@/components/knowledge/knowledge-form";

/** Add a knowledge entry — the shared {@link KnowledgeForm} in "create" mode. */
export const KnowledgeCreate = () => <KnowledgeForm mode="create" />;
