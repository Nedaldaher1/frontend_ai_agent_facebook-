import { ProductForm } from "@/components/products/product-form";

/** Add a product — the shared {@link ProductForm} in "create" mode. */
export const ProductCreate = () => <ProductForm mode="create" />;
